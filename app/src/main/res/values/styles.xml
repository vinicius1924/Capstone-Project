<resources>

    <!-- Base application theme. -->
    <style name = "AppTheme" parent="AppTheme.Base">
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ButtonUnsubscribedTextStyle" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">@color/material_red_700</item>
    </style>

    <style name="ButtonSubscribedTextStyle" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">@color/material_green_700</item>
    </style>

</resources>
